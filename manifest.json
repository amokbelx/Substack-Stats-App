{
  "manifest_version": 3,
  "name": "Substack App Cookie Copier",
  "version": "1.0",
  "description": "One-click copy of your Substack session cookie for the Substack App stats tool. Reads cookies via Chrome's own official extension API — not a security bypass. Works with any Substack publication you're logged into.",
  "permissions": ["cookies", "tabs"],
  "host_permissions": ["*://*.substack.com/*"],
  "action": {
    "default_popup": "popup.html"
  }
}
